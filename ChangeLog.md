# 0.05
- Added file `InstallDmg.sh` and its contents (will need testing to verify functionality).

# 0.04
- Added script contents to `CreateUser.sh` (will need testing to verify functionality and does not include logic for SecureToken logic)

# 0.03
- Added script contents to `CreateGroup.sh` (will need testing to verify functionality)

# 0.02
- Added script contents to `AddToGroup.sh` (will need testing to verify functionality)

# 0.01
- Initial setup.
- Created sh files for scripts to be worked on.